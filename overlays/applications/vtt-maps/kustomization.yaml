apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: vtt-maps-kustomization

namespace: vtt-maps

resources:
  - namespace.yaml
  - certificate.yaml
  - deployment.yaml
  - service.yaml
  - gateway.yaml
  - virtual-service.yaml
